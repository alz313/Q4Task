<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_margin="4dp">

    <TextView
        android:id="@+id/fragment_square_item_tv_id"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:visibility="gone"/>

    <TextView
        android:id="@+id/fragment_square_item_tv_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:maxLines="1"
        android:text="Task #1"
        android:textColor="@color/colorPrimary_text"
        android:textSize="13sp"/>

    <TextView
        android:id="@+id/fragment_square_item_tv_desc"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Task description"
        android:layout_below="@id/fragment_square_item_tv_title"
        android:layout_marginTop="2dp"
        android:maxLines="2"
        android:textColor="@color/colorSecondary_text"
        android:textSize="11sp"/>

</RelativeLayout>